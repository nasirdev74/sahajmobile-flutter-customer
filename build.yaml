targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
        generate_for:
          include:
            - lib/data/models/**/*.dart
            - lib/data/models/*.dart
            - lib/models/*.dart
      injectable_generator:
        generate_for:
          include:
            - lib/**