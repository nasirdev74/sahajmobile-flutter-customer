import 'package:flutter/material.dart';

class InstallmentPlanPage extends StatelessWidget {
  const InstallmentPlanPage({super.key});

  static const route = '/installment_plan_page';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(resizeToAvoidBottomInset: false, backgroundColor: Colors.pinkAccent);
  }
}
